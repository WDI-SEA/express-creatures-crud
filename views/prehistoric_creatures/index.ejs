<ul>
    <% creatureData.forEach( (creat, index) => { %>
      <li>
        <%= creat.type %> looks like <%= creat.img_url %>
        <a href="/prehistoric_creatures/edit/<%= index %>">Edit creature</a>
      </li>
    <% }) %>
  </ul>